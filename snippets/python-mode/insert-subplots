# -*- mode: snippet -*-
# name: insert-subplots
# key: <fig
# --

fig, ax = plt.subplots()

ax.plot(${1:x},
	${2:y},
	$0)

ax.set_xlabel("${3:xlabel}")
ax.set_ylabel("${4:ylabel}")
ax.grid(which="both")
ax.legend()